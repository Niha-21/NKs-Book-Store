spring:
  config:
    activate:
      on-profile: local

  app:
    jwtSecret: QL8GgdMEJPSWn1acAmX1imY9koBWB+Ovmwq79dGA4xox1/t8eNQ3X8U2BujZOGuDoZ34PxLryK+NMS72ECOMnA==
    
  datasource:
    url: jdbc:h2:mem:userdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console/

  data:
    redis:
      host: localhost
      port: 6379

origin:
  UI: http://localhost:5173

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
